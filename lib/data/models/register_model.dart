//TODO
/*
import 'dart:convert';

RegisterModel hashModelFromJson(String str) => RegisterModel.fromJson(json.decode(str));
String hashModelToJson(RegisterModel data) => json.encode(data.toJson());

class RegisterModel {
  RegisterModel({
    required this.hash,
  });

  String hash;

  factory RegisterModel.fromJson(Map<String, dynamic> json) => RegisterModel(
    hash: json["hash"],
  );

  Map<String, dynamic> toJson() => {
    "hash": hash,
  };
}

*/
